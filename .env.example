# API port (default: 3000)
API_PORT=3000

# Frontend port (default: 80)
FRONTEND_PORT=80

# API URL for frontend to connect to backend
# In Docker Compose, this is set to http://api:3000
# For local development, use http://localhost:3000
API_URL=http://api:3000